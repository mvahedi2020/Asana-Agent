import {test,expect} from '@playwright/test';
import {seedTasks} from '../src/logic';
test('preserves incompatible saved state until an explicit reset',async({page})=>{await page.addInitScript(()=>localStorage.setItem('northstar.asana-agent.v1',JSON.stringify({version:2,tasks:[]})));await page.goto('./');await expect(page.getByRole('status')).toContainText('left untouched');expect(await page.evaluate(()=>JSON.parse(localStorage.getItem('northstar.asana-agent.v1')!).version)).toBe(2);await page.getByRole('button',{name:'Reset sample',exact:true}).click();await page.getByRole('button',{name:'Confirm change'}).click();expect(await page.evaluate(()=>JSON.parse(localStorage.getItem('northstar.asana-agent.v1')!).version)).toBe(1);});
test('invalid stored dates recover to the sample without a render crash',async({page})=>{await page.addInitScript(tasks=>localStorage.setItem('northstar.asana-agent.v1',JSON.stringify({version:1,tasks})),[{...seedTasks[0],due:'2026-99-99'}]);await page.goto('./');await expect(page.getByRole('heading',{name:'Make the work legible.'})).toBeVisible();await expect(page.getByRole('status')).toContainText('incompatible');});
